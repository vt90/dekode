{
  "name": "dekodeapi",
  "version": "1.0.0",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "prebuild": "yarn docs",
    "build": "rimraf dist/ && babel src --out-dir dist/ --ignore *.spec.js --copy-files && cpx .env dist/ && cpx .env.example dist/",
    "prestart": "yarn docs",
    "start": "nodemon --exec babel-node src/index.js --ignore tmp/* --ignore README",
    "predocs": "rimraf src/public/swagger.json",
    "docs": "apidoc-swagger -i src -o public/apidoc",
    "postdocs": "cpx public/apidoc/swagger.json src/public/ && rimraf public/apidoc"
  }
}
